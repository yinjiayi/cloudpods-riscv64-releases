# Copy to rpm/kvm-validation.env only after the exact RPM build has passed
# rpm/verify-qemu-kvm-rpm.sh on a physical RISC-V KVM host.
RPM_BUILD_RUN_ID=REPLACE_WITH_RPM_BUILD_WORKFLOW_RUN_ID
QEMU_RPM_SHA256=REPLACE_WITH_KVM_VALIDATION_SHA256
