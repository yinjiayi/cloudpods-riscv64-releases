# Contributing

Changes must keep every upstream source pinned by a full commit or SHA-256
digest in `versions.env`. Do not commit generated RPMs, container archives,
credentials, or GitHub tokens.

Before submitting a change, run the shell and workflow syntax checks described
in `README.md`. Commits must carry a Developer Certificate of Origin sign-off:

```bash
git commit --signoff
```
